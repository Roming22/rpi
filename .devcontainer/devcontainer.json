{
    "build": {
        "dockerfile": "../tools/container/Containerfile",
        "context": ".."
    },
    "mounts": [
        "source=${localEnv:HOME}/.ssh,target=/home/dev/.ssh,type=bind,consistency=consistent"
    ]
}